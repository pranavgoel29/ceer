appId: com.ceer.app
productName: Ceer
copyright: Copyright © 2026
electronVersion: "35.7.5"
npmRebuild: false
artifactName: Ceer-${version}-${arch}.${ext}

publish:
  provider: github
  releaseType: release

directories:
  output: dist-out
  buildResources: resources

files:
  - dist-electron/**/*
  - package.json
  - "!**/*.map"

extraResources:
  - from: ../web/dist
    to: web
    filter:
      - "**/*"
  - from: resources/icon.png
    to: icon.png
  - from: resources/icon.ico
    to: icon.ico
  - from: resources/icon.iconset/icon_16x16@2x.png
    to: tray-icon.png

asar: true

# Ad-hoc sign macOS bundles when no Apple Developer cert (CSC_*) is configured.
afterPack: scripts/after-pack-adhoc-sign.cjs

mac:
  category: public.app-category.video
  icon: resources/icon.icns
  # electron-builder 25 treats identity "-" as a keychain name; ad-hoc sign runs in afterPack instead.
  identity: null
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: resources/entitlements.mac.plist
  entitlementsInherit: resources/entitlements.mac.inherit.plist
  target:
    - dmg
    - zip

dmg:
  title: Ceer

win:
  icon: resources/icon.ico
  target:
    - nsis
  sign: false
  # Embed icon + version metadata via rcedit; signing is skipped when no certificate is configured.
  signAndEditExecutable: true

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
